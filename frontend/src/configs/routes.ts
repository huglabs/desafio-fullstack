export const API_ROUTES = {
    
    AUTH_BROADCAST:{
        AUTH: "/broadcasting/auth",
    },  

    AUTH: {
        LOGIN: "/login",
        REGISTER: "/register",
        LOGOUT: "/logout",
    }

} as const;